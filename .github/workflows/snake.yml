name: Generate snake animation

on:
  # run automatically every 24 hours
  schedule:
    - cron: "0 */24 * * *"
  
  # allows to manually run the job at any time
  workflow_dispatch:
  
  # run on every push on the master branch
  push:
    branches:
    - main

jobs:
  generate:
    permissions: 
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      # generates a snake game from a github user contributions graph
      - name: generate snake animation
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: akangumam
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#00ff00&color_dots=#bfd6f6,#8dbdff,#64a1f4,#4b91f1,#3c7dd9
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=#00ff00
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      # generate pacman style animation with different colors
      - name: generate pacman animation  
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: akangumam
          outputs: |
            dist/pacman-contribution-graph.svg?color_snake=#ffff00&color_dots=#ff6b6b,#ff8e53,#ff006e,#8338ec,#3a86ff&background_color=#000000
            dist/pacman-contribution-graph-dark.svg?palette=github-dark&color_snake=#ffff00&color_dots=#ff6b6b,#ff8e53,#ff006e,#8338ec,#3a86ff
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      # push the content of <build_dir> to a branch
      - name: push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
          keep_history: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}